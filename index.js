const { test, assert, affirm } = require('./lib/t3st')
const { result_text, tally_results } = require('./lib/text')

module.exports = {
    test
    , assert
    , affirm
    , result_text
    , tally_results
}
